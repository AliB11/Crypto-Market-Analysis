# بازبینی نهایی لایه‌ی داده و مشتقات (پیش از انتقال به گیت‌هاب)

تاریخ: ۲۰۲۶-۰۹-۱۸
شاخه: `arena/01a0b186-crypto-market-analysis` (PR #9)
روش: اجرای واقعی `app.js` در Node با DOM ساختگی + آزمون‌های تصادفی‌سازی‌شده (fuzz) + سنجش مسیرهای تصمیم در بک‌تست.

## نتیجه

**چهار ایراد منطقی پیدا و اصلاح شد؛ هیچ‌کدام در نسخه‌ی منتشرشده‌ی PR #9 باقی نمانده‌اند.**
هیچ ادعای سودآوری در این گزارش نیست؛ همه‌ی اعداد از داده‌ی ساختگی کنترل‌شده می‌آید.

| # | یافته | چرا مهم بود | اصلاح | آزمون قفل‌کننده |
|---|---|---|---|---|
| ۱ | **قفل‌شدن صف غنی‌سازی** — ارز غنی‌شده از فهرست نامزدها بیرون نمی‌رفت، هر چرخه اول انتخاب می‌شد و `runEnrichment` با ۰ برمی‌گشت؛ پس صف هرگز به بقیه نمی‌رسید | پوشش داده روی **۳ از ۹** ارز متوقف می‌شد؛ ATR/حجم برای بقیه هرگز نمی‌آمد | نامزدهای دارای کش تازه (هر دو منبع) از فهرست حذف می‌شوند + دفاع دوم در حلقه‌ی صف (تا ۵ پرش در هر چرخه) | «صف غنی‌سازی قفل نمی‌شود» — قبل از اصلاح ۳/۹، بعد از اصلاح ۶/۶ |
| ۲ | **خاموشی بی‌صدای قاعده‌ی ازدحام** — `classifyCrowding` کلیدهای `oiRisingPct`، `rsiLongCrowd`، `rsiShortCrowd` را از `THRESHOLDS` می‌خواند که وجود نداشتند | مقایسه با `undefined` همیشه false ⇒ **کل تشخیص ازدحام هرگز فعال نمی‌شد** (خروجی `crowd:null` در همه‌ی حالت‌ها) | افزودن کلیدها به `THRESHOLDS` + ادغام دفاعی `Object.assign({}, THRESHOLDS, th)` تا شیء ناقص، قاعده را نکشد | «آستانه‌های ازدحام از THRESHOLDS می‌آیند» (۴ حالت، شامل آستانه‌ی سفارشی ناقص) |
| ۳ | **تکرار داده‌ی سنگین در کش تحلیل** — بلوک `md` (کندل ۴ساعته + سری ساعتی) به‌ازای هر ارز در `cb_cache` هم ذخیره می‌شد، در حالی که لایه‌ی داده خودش کش دارد | payload ذخیره‌سازی چند برابر و داده‌ی کهنه به تحلیل چسبیده می‌ماند | کش تحلیل «سبک» ذخیره می‌شود؛ در بارگذاری مجدد، `attachCachedMarketData` داده را از کش خودش برمی‌گرداند (بدون فراخوان تازه) | «کش تحلیل `md`/`ohlc` ندارد ولی بارگذاری مجدد داده را برمی‌گرداند» (`hits.ohlc=0`، `hits.chart=0`) |
| ۴ | **شکستن برنامه در نبود ماژول‌های تازه** — اگر `analytics.js`/`market-data.js` بارگذاری نمی‌شد، استثنا بالا می‌آمد | کاربر با نسخه‌ی کش‌شده‌ی HTML یا خطای شبکه، برنامه‌ی سفید می‌گرفت | همه‌ی مسیرها با گارد `typeof` و مسیر جانشین امن؛ آزمون با حذف عمدی ماژول‌ها | «نبود ماژول‌های لایه‌ی داده برنامه را نمی‌شکند» (۳ ترکیب) |

## بررسی‌های بدون یافته (شاهد اجرا‌شده)

| حوزه | روش | نتیجه |
|---|---|---|
| هندسه‌ی پلن خرید (ورود/پلکان/حد ضرر/اهداف) | fuzz روی ۴۰ بازار تصادفی × ~۱۰ ارز (۳۸۲ ارز، نیمی غنی‌شده) | ۰ نقض: `stop<avgEntry`, ریسک ≤۳۰٪, `tp2>tp1>entry`, `0.7mkt ≤ entry ≤ mkt`, پلکان نزولی |
| سقف ریسک در پالایش ATR | تحلیل بازه + fuzz | `atrLike` در بازه‌ی ۰.۸–۹٪ بسته شده؛ بدترین حالت ۱۹.۸٪ < سقف ۳۰٪ ⇒ تضعیف کف، ممکن نیست |
| مسیر تخریب نرم (بدون داده/آفلاین/کش کهنه) | اجرای زنجیره‌ی کامل، سپس رندر مودال/لیست/CSV/JSON | هیچ `NaN`/`undefined`/`Infinity` در UI؛ `mdQuality='base'`, `atr=null`, قواعد تازه خاموش |
| تکرار رویدادهای تایپ‌شده | ۶ چرخه‌ی پیوسته با مشتقات ثابت | تعداد اعلان ثابت (۱)، هیچ متن تکراری (گذار حالت با `mon.prev` درست کار می‌کند) |
| کلید مشتقات بر پایه‌ی symbol پایه | دو صرافی برای یک دارایی (`SOLUSDT` + `SOL-USDT`) | یک کلید، `venues=2`، میانه‌ی فاندینگ/بازار درست |
| تفکیک بودجه‌ی مشتقات از OHLCV | چرخه‌های پیوسته با `resetBudget` | فراخوان مشتقات فاصله‌ی مستقل دارد؛ صف کندل از مشتقات گرسنه نمی‌شود |

## پوشش مسیرهای تصمیم در بک‌تست (جدید)

`tests/backtest.mjs` حالا جدول پوشش چاپ می‌کند تا «سبزی» آزمون به‌معنای «اجرا شدن مسیر» باشد. تاریخچه‌ی OI هم در حافظه‌ی جعلی کاشته می‌شود تا شاهد اصلی ازدحام واقعاً محاسبه شود:

| بازو | پنجره | ارز×پنجره | دروازه باز | انتخابی | مسدود | داده کامل | فاندینگ | ازدحام |
|---|---|---|---|---|---|---|---|---|
| الف: بدون لایه‌ی داده | ۴۸ | ۲۸۸ | ۰ | ۹۱ | ۱۹۷ | ۰ | ۰ | ۰ |
| ب: با لایه‌ی داده | ۴۸ | ۲۸۸ | ۱ | ۳۳ | ۲۵۴ | ۱۰۹ | ۲۸۸ | ۷۵ |

زیرگروه ازدحام (بازوی ب، نمونه‌ی ساختگی و محدود):

| زیرگروه | n | برد٪ | انتظار R | PF |
|---|---|---|---|---|
| ازدحام داغ | ۱۰ | ۰.۰ | −۰.۸۰ | ۰.۰۰ |
| ازدحام گرم | ۳۵ | ۵۷.۱ | −۰.۰۲ | ۰.۹۴ |
| بدون ازدحام | ۱۴۳ | ۶۰.۱ | +۰.۱۵ | ۱.۴۲ |

جهت رابطه (ازدحام داغ بدتر از بی‌ازدحام) با فرضیه‌ی طراحی سازگار است، ولی نمونه کوچک و هم‌خط با رژیم بازار است؛ **شاهد قطعی نیست**. سیگنالی که «فقط» به دلیل ازدحام رد شده باشد در این اجرا رخ نداد و همین در خروجی آزمون صریح گفته می‌شود.

## محدودیت‌ها (بدون تغییر)

- داده‌ی بک‌تست ساختگی و کنترل‌شده است، نه تاریخی؛ اعتبارسنجی روی داده‌ی واقعی انجام نشده.
- ATR از کندل ۴ساعته می‌آید؛ تایم‌فریم پایین‌تر پوشش داده نمی‌شود.
- ابزار مرورگر (Playwright) در محیط ساخت نصب نیست؛ رابط کاربری فقط با DOM ساختگی و آزمون دروازه سنجیده شده، نه در مرورگر واقعی.
- هیچ بخشی از این لایه، ادعای سودآوری یا آمادگی معامله‌ی واقعی را ندارد.
