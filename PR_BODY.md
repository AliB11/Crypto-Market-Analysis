# شفاف‌سازی سیگنال‌های مجاز + بهبود موبایل + مستندسازی واگرایی‌ها

پیرو بازبینی خواسته‌شده، سه مورد رسیدگی شد:

## ۱) مستندسازی فنی واگرایی‌ها (بدون تغییر منطق)
محل‌های اثر واگرایی‌های RSI (RD±/HD±) در README مستند شد: تشخیص در `analyze()` روی داده‌ی ساعتی ۷ روزه، اثر روی امتیاز تکنیکال، طبقه‌بندی «مستعد رشد»، `bias` بهترین قیمت خرید، امتیاز فرصت خرید و نمایش (مودال + ستون مقایسه).

## ۲) رفع ابهام «کدام سیگنال‌ها مجاز به معامله‌اند»
پیش‌تر چند فهرست سیگنال هم‌زمان وجود داشت و مشخص نبود کدام‌ها واقعاً مجازند:

- پنل «🚀 قوی‌ترین سیگنال‌های بازار» → **«✅ سیگنال‌های اصلی — مجاز برای معامله»**: فقط ارزهای عبورکرده از دروازه (وضعیت `open`) با نشان دروازه؛ اگر دروازه بسته باشد به‌جای فهرست، پیام صریح «ورود تازه توصیه نمی‌شود» نمایش داده می‌شود. در حالت «دروازه خاموش» با توضیح مناسب به فهرست قوی‌ترین‌ها برمی‌گردد.
- **نوار «سیگنال‌های مجاز برای معامله»** در بالای بخش بهترین‌ها: چیپ‌های قابل کلیک ارزهای مجاز + شمارش مجاز/انتخابی + وضعیت لحظه‌ای دروازه + موارد «در آستانه‌ی مجوز».
- جدول ۲۵ ارز برتر: ردیف‌های مجاز **خط سبز کناری** دارند و ردیف‌های مسدود **کم‌رنگ** شده‌اند.
- چیدمان عنوان بخش بهترین‌ها مرتب شد (راهنمای طولانی به خط مستقل رفت).

## ۳) بهبود نمایش موبایل
- ستون اول جدول‌ها در اسکرول افقی **ثابت (sticky)** شد — نام ارز همیشه دیده می‌شود
- هدر فشرده‌تر (لوگوی کوچک‌تر، دکمه بروزرسانی فقط آیکونی، حذف توضیح لوگو)
- کارت‌های نمای کلی ۲ستونه، چیپ‌های دسته‌بندی اسکرول‌شونده، جستجوی تمام‌عرض
- مودال و فونت‌ها برای صفحات کوچک بهینه شدند؛ `۱۶px` شدن ورودی‌ها برای جلوگیری از زوم خودکار iOS
- رفع باگ جهت‌دهی مدال کارت‌های برترین در RTL (`padding-left` → `padding-inline-start`)

## آزمون‌ها
```bash
node tests/gate.test.mjs   # ۱۱ آزمون ✅
```
رندر نوار و پنل جدید در دو سناریوی ریسک‌پذیر/ریسک‌گریز (دروازه باز/بسته) به‌صورت دستی با همان هارنس آزمون‌ها راستی‌آزمایی شد.
