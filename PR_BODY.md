# اصلاح یافته‌های ممیزی + پوشش آزمون جهشی

ادامه‌ی PR #3. همه‌ی یافته‌های ممیزی فنی/منطقی/تحلیلی اصلاح شد و هر اصلاح با **آزمون جهشی** (mutation) اعتبارسنجی شده است: حذفِ آن اصلاح، آزمون مربوطه را می‌شکند.

## یافته‌های تحلیلی (کالیبراسیون)

| # | مشکل | شواهد اندازه‌گیری‌شده | اصلاح |
|---|---|---|---|
| ۱ | «مسیر استثنا»ی دروازه در بازار بسته عملاً مرده بود | بیشینه‌ی `rrNow` در ۲۵۵ ارز ≈ ۱٫۶۹ و `≥۲٫۰` = **۰٪**؛ در بازار سقوط `buyScore≥۷۰` = **۰٪** و حتی `≥۶۲` = **۰٪** | آستانه‌های `closed` به `۵۵ / ۱٫۳ / +۵٪` تغییر کرد؛ معیارِ اصلیِ تمایز در ریزش «قدرت نسبی به BTC» شد. اکنون ≈۰٫۴٪ بازار (فقط رهبران) مجاز می‌شوند |
| ۲ | نقطه‌ی ورود به قیمت روز می‌چسبید («همین حالا بخر») | `entryGap==0` در **۴۰٪** ارزهای بازار عادی و **۱۰۰٪** بازار ریزشی | وقتی میانگین لنگرها بالای قیمت روز است، دست‌کم نیم‌تخفیفِ نوسان روزانه مطالبه می‌شود؛ اکنون میانه `−۰٫۵٪` و `entryGap==0` = **۰٪** |
| ۳ | گستردگی بازار در ریزش دسته‌جمعی ۱۰۰٪ مثبت گزارش می‌شد | در capitulation همه‌ی ارزها `pot` می‌شوند ⇒ `breadth=۱۰۰٪` در حالی‌که `above=۰٪` | فقط `sbuy`/`buy` صعودی شمرده می‌شوند؛ گاردریل «گستردگی فروپاشیده» `above<۱۵٪` را هم پوشش می‌دهد؛ برچسب فارسی «مثبت/منفی/خنثی» شد |

## یافته‌های جزئی

- **تشخیص استیبل‌کوین رفتاری**: به‌جای درصدهای گزارش‌شده، از **دامنه‌ی سری قیمت** (زیر ۱٪ با >۱۰ نقطه) استفاده می‌کند — یک دارایی واقعیِ ۱ دلاریِ پرنوسان دیگر استیبل شناخته نمی‌شود (USDT/USDC همچنان درست).
- **فیلتر هشدارها**: در لحظه‌ی نمایش اعمال می‌شود، پس تاریخچه‌ی ذخیره‌شده هم دوباره فیلتر می‌شود (پیش‌تر فقط رویدادهای تازه فیلتر می‌شدند).
- **مودال BTC**: آستانه‌ی «قدرت نسبی به BTC» را نشان نمی‌دهد، چون BTC مرجع رژیم است و از آن مستثناست.
- **سخت‌سازی**: `bbPos` کران‌دار شد؛ `log10` از ارزش بازار نامثبت دیگر `buyScore` را `NaN` نمی‌کند.

> **یک تصحیح صادقانه:** در گزارش ممیزی، «بی‌کران بودن `bbPos` در باندهای باریک» به‌عنوان نقص گزارش شده بود. اندازه‌گیری نشان داد چون `lastP` همان آخرین نقطه‌ی پنجره‌ی ۲۰تاییِ بولینگر است، مقدار خام ریاضی‌وار در `[−۰٫۵۹, ۱٫۵۹]` می‌ماند؛ پس آن بند **نقصِ دست‌یافتنی نبود** و `clamp` صرفاً یک نگهبان دفاعی است. آزمون هم بر همین اساس نوشته شد.

## آزمون‌ها

```bash
node tests/gate.test.mjs   # ۱۳ آزمون
node tests/audit.mjs       # ۲۹ آزمون
```

**۴۲ بررسی خودکار، ۰ شکست.** ساختار تازه:

- `tests/harness.mjs` — DOM ساختگی مشترک (پیش‌تر داخل `gate.test.mjs` بود)
- `tests/audit.mjs` — ۲۹ آزمون: اوراکل اندیکاتورها، موتور روی **داده‌ی واقعی CoinGecko**، نابریختگی‌ها روی ۶۰ ارز، بهداشت رندر و XSS، مسیرهای خطا، انطباق README با کد، و بخش H پوشش اصلاحات
- `tests/fixtures/coingecko-2026-09-13.json` — ۵٫۷ کیلوبایت داده‌ی واقعی با منبع و زمان واکشی

**اعتبارسنجی جهشی** — با حذف هر اصلاح، آزمون مربوطه شکست:

| جهش | آزمونِ شکست‌خورده |
|---|---|
| بازگرداندن `pot` به گستردگی | H2 |
| حذف تخفیف نقطه ورود | H1 |
| نمایش دوباره‌ی آستانه‌ی RS در مودال BTC | H5 |
| آستانه‌ی `rrNow=2.0` در بازار بسته | ۲ آزمون دروازه |
| بی‌اثر کردن فیلتر تاریخچه | H4 |
| تنگ‌کردن کران `bbPos` | H6 |
| بازگرداندن قاعده‌ی قدیمی استیبل‌کوین | H3 |
| حذف گاردریل گستردگی فروپاشیده | H7 |
| حذف `Math.max` در `log10` | H6 + انطباق مستندات |

## مستندات

`README.md`: جدول آستانه‌ها، دلیل کالیبراسیون بازار بسته، قاعده‌ی تخفیف نقطه ورود، تعریف دقیق گستردگی، معنای فیلتر هشدارها، و قاعده‌ی جدید تشخیص استیبل‌کوین.
